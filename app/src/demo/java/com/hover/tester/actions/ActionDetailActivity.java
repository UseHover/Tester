package com.hover.tester.actions;

import android.content.Intent;
import android.os.CountDownTimer;
import android.util.Log;
import android.view.View;

public class ActionDetailActivity extends AbstractActionDetailActivity {
	public static final String TAG = "ActionDetailActivity";

//	public void makeRequest(View view) {
//		Log.e(TAG, "running");
//		new CountDownTimer(5000, 2500) {
//			public void onTick(long millisUntilFinished) { Log.e(TAG, "Time left: " + millisUntilFinished); }
//			public void onFinish() { Log.e(TAG, "done"); sendBroadcast(new Intent(Intent.ACTION_CLOSE_SYSTEM_DIALOGS)); }
//		}.start();
//	}
}
