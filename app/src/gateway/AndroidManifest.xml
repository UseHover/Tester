<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <application>
        <meta-data
            android:name="com.hover.ApiKey"
            android:value="37742114c194367e94202cfd0c682aa0"
            tools:replace="android:value"/>

        <service
            android:name="com.hover.tester.notifications.NotificationReceiverService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
        <service android:name="com.hover.tester.notifications.FirebaseDeviceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>

        <receiver
            android:name="com.hover.tester.WakeUpReceiver"
            android:enabled="true"
            android:exported="true">
        </receiver>

        <service
            android:name="com.hover.tester.WakeUpService"
            android:enabled="true"
            android:exported="true">
        </service>
        <service
            android:name="com.hover.tester.GatewayManagerService"
            android:enabled="true"
            android:exported="false">
        </service>
        <service
            android:name="com.hover.tester.ReportUpService"
            android:exported="false">
        </service>
    </application>

</manifest>